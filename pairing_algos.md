Role: You are an expert PyTorch performance engineer and AI researcher specializing in Generative Flow Networks, Diffusion Models, and Optimal Transport.Task: Implement a highly efficient, vectorized PyTorch module/function that pairs a batch of noise samples ($X_0$) with a batch of data samples ($X_1$) based on a globally configurable pairing_strategy. This is the preprocessing step for constructing target vector fields in Continuous Normalizing Flows / Flow Matching.Input Specifications:X0 (torch.Tensor): A batch of noise samples. Shape: (B, ...) where B is batch size, and ... are arbitrary dimensions (e.g., C, H, W for images or D for flat vectors).X1 (torch.Tensor): A batch of target data samples. Shape: (B, ...). Matches X0 dimensions.strategy (str): One of ['random', 'sliced_sorting', 'minibatch_ot'].Output Specifications:Return X0_paired, X1_paired (torch.Tensor, torch.Tensor): The matched tensors, maintaining the original shapes, ready for ODE target computation: $V_{target} = X_1 - X_0$.Implementation Constraints & Algorithm Rules:1. random (Independent Coupling)Mechanism: Shuffle the batch indices of X1 randomly relative to X0.Efficiency: Use torch.randperm on the batch dimension B. This must be strictly $O(1)$ overhead relative to tensor size. No loops.2. sliced_sorting (1D Sliced Optimal Transport)Mechanism: Approximate OT by sorting.Efficiency: 1. Flatten X0 and X1 to shape (B, -1).2. Take the mean or sum across the feature dimension to reduce to shape (B,) (a 1D projection). Alternatively, project onto a random unit vector.3. Use torch.argsort to find the rank indices for both X0 and X1 based on this 1D value.4. Rearrange X0 and X1 so that their sorted indices align.Performance: Must remain entirely on the GPU. Avoid any .cpu() calls.3. minibatch_ot (Mini-Batch Exact Optimal Transport)Mechanism: Find the exact bipartite matching that minimizes the total $L_2$ distance between pairs within the batch.Efficiency: 1. Flatten X0 and X1 to (B, D).2. Compute the pairwise cost matrix efficiently using torch.cdist(X0, X1, p=2).3. Crucial: To solve the linear assignment problem (Hungarian algorithm), you must detach the cost matrix and move it to the CPU to use scipy.optimize.linear_sum_assignment.4. Return the reordered X1 tensor based on the optimal column indices returned by SciPy.5. Bonus points: If the batch size is exceptionally large, implement an optional highly vectorized GPU-native Sinkhorn iteration approximation to avoid the CPU bottleneck, but default to exact Hungarian for standard mini-batches (e.g., B <= 256).Safety & Performance Checklist:[ ] Wrap the entire pairing logic in @torch.no_grad() to prevent memory leaks and graph retention. This is a purely static pairing step.[ ] Ensure the device placement (.to(device)) is preserved. The output tensors must be on the exact same device as the input tensors.[ ] Do not mutate the original X0 and X1 tensors in-place.[ ] Write robust assertions at the top of the function to guarantee X0.shape == X1.shape.Write the complete, well-documented Python code using PyTorch. Include brief inline comments explaining the complexity (e.g., $O(N \log N)$ for sorting, $O(N^3)$ for Hungarian) and physical intent of the operations.